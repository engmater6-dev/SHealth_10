# Phase 02 — texttest_fixture.py (stdout 캡처)

> **선행:** Phase 01 설계 확정  
> **다음:** [`03.phase_golden_test.md`](03.phase_golden_test.md)

---

## 프롬프트

```
@src/main/python/shealth_bmi.py @src/main/python/shealth.py
@src/test/python/conftest.py @src/test/python/test_cli_main.py
@doc/golden_master_plan.md

[P] 회귀 테스트(Approval/Golden Master) 설계 전문가입니다.

[C] Python 3, pytest
    - 외부 TextTest 프레임워크 의존성 추가 금지 (표준库 + pytest만)
    - 모듈명: texttest_fixture.py (TextTest *스타일* 헬퍼, 패키지 설치 아님)

[T] texttest_fixture.py 출력 캡처 API를 설계·구현해줘.
    1) 기준 출력(golden) 생성/보관 헬퍼
       - capture_cli_stdout(data_file: Path, *, use_subprocess: bool) -> str
       - subprocess 방식: 프로젝트 루트 cwd, `sys.executable` + shealth_bmi.py 경로
       - in-process 방식: io.StringIO 또는 pytest capsys 패턴 재사용
       - logging.basicConfig 출력이 stdout에 섞이지 않도록 main 호출 전략 명시
    2) 정규화 함수
       - normalize_output(text: str) -> str  # strip trailing whitespace, CRLF→LF
    3) golden 파일 읽기/쓰기
       - read_golden(path: Path) -> str
       - write_golden(path: Path, content: str) -> None
    4) docstring: Phase 03 test_golden_master.py에서 사용 예시 3줄

[F]
    - src/test/python/texttest_fixture.py
    - (선택) src/test/golden/.gitkeep
    - 단위 테스트 1~2건: 작은 CSV로 캡처 길이·줄 수 assert (golden 비교 아님)
```

---

## 완료 기준 (구현 시)

- [ ] `shealth.dat` 대상 캡처 시 6줄 non-empty
- [ ] subprocess / in-process 중 설계서 선택안 구현
- [ ] import 경로: test에서 `from texttest_fixture import ...` 동작

---

*프롬프트만 등록 — 구현 미진행*
