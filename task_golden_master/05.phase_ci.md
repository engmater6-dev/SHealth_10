# Phase 05 — GitHub Actions CI (pytest 자동 실행)

> **선행:** Phase 03·04 Green  
> **다음:** `prompt/05.test_case.md` 또는 `report/05.test_case.md` TC #44 완료 반영

---

## 프롬프트

```
@README.md @requirements-dev.txt
@src/test/python/ @.cursorrules
@src/test/golden/shealth_dat_stdout.txt

[P] 회귀 테스트(Approval/Golden Master) 설계 전문가 + CI 구성 담당입니다.

[C] Python 3.8+, pytest, pytest-cov, GitHub Actions

[T] CI(GitHub Actions)에서 pytest 자동 실행을 구성해줘.
    1) `.github/workflows/pytest.yml` (신규)
       - trigger: push, pull_request (branches: main, refactoring, tc 등)
       - job: ubuntu-latest + windows-latest (선택 1개면 ubuntu만)
       - steps:
         - checkout
         - setup-python 3.12 (또는 3.8 matrix)
         - pip install -r requirements-dev.txt
         - pytest 단위 (golden 제외): `pytest --cov=... --cov-fail-under=90`
         - pytest golden: `pytest -m golden_master -v` (커버리지 생략 가능)
    2) golden 파일이 repo에 없으면 CI **실패** (approve 최초 생성은 로컬)
    3) `UPDATE_GOLDEN`은 CI에서 **금지** (env 미설정)
    4) (선택) PR 코멘트에 커버리지 요약 — 최소 범위만

[F]
    - `.github/workflows/pytest.yml`
    - README에 CI 배지·상태 문단 2줄 (선택)
    - doc/golden_master_plan.md § CI 절 추가
```

---

## 완료 기준 (구현 시)

- [ ] PR/push 시 unit + golden job Green
- [ ] golden diff 시 CI 실패·로그에 diff 일부 출력
- [ ] `shealth.dat` 경로가 CI cwd(루트)에서 resolves

---

*프롬프트만 등록 — 구현 미진행*
