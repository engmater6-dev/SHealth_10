# Phase 03 — test_golden_master.py (approve 비교)

> **선행:** Phase 02 `texttest_fixture.py`  
> **다음:** [`04.phase_pytest_markers.md`](04.phase_pytest_markers.md)

---

## 프롬프트

```
@texttest_fixture.py @src/main/python/shealth_bmi.py
@src/test/python/texttest_fixture.py
@shealth.dat @doc/test_plan.md

[P] 회귀 테스트(Approval/Golden Master) 설계 전문가입니다.

[C] Python 3, pytest
    - @pytest.mark.golden_master 필수
    - 기본 pytest 실행 시 golden 제외 가능하도록 마커 설계(Phase 04 연동)

[T] pytest에서 actual 출력과 golden 파일을 비교하는 TC를 설계·구현해줘.
    1) golden_master_expected.txt (또는 src/test/golden/shealth_dat_stdout.txt) 경로 상수
    2) approve 패턴 구현
       ```python
       def approve_output(actual: str, golden_path: Path) -> None:
           if os.environ.get("UPDATE_GOLDEN") == "1":
               write_golden(golden_path, actual)
               return
           if not golden_path.exists():
               write_golden(golden_path, actual)  # 최초 생성 + pytest.warn 또는 skip 아님
               pytest.fail(f"Golden created at {golden_path}. Re-run with file committed.")
           expected = read_golden(golden_path)
           assert normalize(actual) == normalize(expected)
       ```
       - 대안: 최초 없을 때 자동 생성 후 fail → CI에서는 반드시 파일 커밋되어 있어야 함
    3) test_golden_master_shealth_dat_stdout
       - 입력: 루트 shealth.dat (Path(__file__).resolve().parents[3] / "shealth.dat")
       - actual = capture_cli_stdout(...)
       - approve_output(actual, GOLDEN_PATH)
    4) 실패 시 diff 출력 (unified_diff) — 디버깅용

[F]
    - src/test/python/test_golden_master.py
    - src/test/golden/shealth_dat_stdout.txt (UPDATE_GOLDEN=1로 최초 생성 후 커밋)
    - README 또는 doc/golden_master_plan.md에 재생성 명령 1줄:
      `set UPDATE_GOLDEN=1 && python -m pytest ... -m golden_master`
```

---

## 완료 기준 (구현 시)

- [ ] TC #44 충족
- [ ] 의도 없는 golden 변경 시 CI/local assert 실패
- [ ] `UPDATE_GOLDEN=1`로만 기준 파일 갱신 가능

---

*프롬프트만 등록 — 구현 미진행*
