# Phase 04 — pytest.ini · conftest Golden Master 마킹

> **선행:** Phase 03 `test_golden_master.py`  
> **다음:** [`05.phase_ci.md`](05.phase_ci.md)

---

## 프롬프트

```
@src/test/python/conftest.py @src/test/python/test_golden_master.py
@.cursorrules @README.md

[P] 회귀 테스트(Approval/Golden Master) 설계 전문가입니다.

[C] Python 3, pytest

[T] Golden Master 테스트 마킹·실행 프로필을 구성해줘.
    1) pytest.ini (프로젝트 루트, 없으면 생성)
       ```ini
       [pytest]
       testpaths = src/test/python
       markers =
           golden_master: Golden Master stdout approval tests (TC #44)
       addopts = -m "not golden_master"
       ```
       - addopts로 **기본은 golden 제외** — 로컬 빠른 루프
       - 전체 실행: `pytest -m ""` 또는 `pytest -m golden_master` / `--golden`
    2) conftest.py 보강
       - `pytest_configure`: 미등록 marker 경고 방지
       - (선택) `--golden` cli option → `-m golden_master` 전달
       - `golden_dir` fixture: Path to src/test/golden
    3) README § 테스트 실행에 3줄 추가
       - 일반: `python -m pytest src/test/python -v`
       - golden 포함: `python -m pytest src/test/python -v -m golden_master`
       - golden만: `python -m pytest src/test/python -v -m golden_master --no-cov` (선택)

[F]
    - pytest.ini (또는 pyproject.toml [tool.pytest.ini_options] — 기존 convention 따름)
    - conftest.py 패치
    - README 테스트 섹션 패치 (명령만)
```

---

## 완료 기준 (구현 시)

- [ ] `pytest` 기본 실행 시 golden TC 수집 제외 또는 skip 없이 deselect
- [ ] `-m golden_master` 시 test_golden_master만/포함 실행
- [ ] marker 미사용 TC 45건 회귀 없음

---

*프롬프트만 등록 — 구현 미진행*
