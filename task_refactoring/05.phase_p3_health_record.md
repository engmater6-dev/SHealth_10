# Phase 05 — P3: `HealthRecord` dataclass·나이대 통합

> **README:** §2 P3 — dataclass, `AGE_BANDS`, 캡슐화  
> **선행:** Phase 04 Green · **다음:** [`06.phase_p4_cli_entrypoint.md`](06.phase_p4_cli_entrypoint.md)

---

## 프롬프트

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. Primitive Obsession·중복 나이대 루프를 제거합니다.

[C] 제약
    - `calculate_bmi` / `get_bmi_ratio` 공개 API 유지
    - 외부에서 `shealth.ages` 등을 참조하는 테스트가 있으면 property로 하위 호환
    - `_bmi_ratios` 키 `(int, int)` 유지
    - 테스트 Green

[T]
    1) `models.py` 또는 `shealth.py`에:
       ```python
       @dataclass
       class HealthRecord:
           id: int
           age: int
           weight: float
           height: float
       ```

    2) 내부 상태: `self._records: list[HealthRecord]` (주 저장소)
       - 단계적 마이그레이션: property `ages` → `[r.age for r in self._records]`
       - 또는 리팩토링 완료 후 평행 리스트 제거 (테스트 확인 후)

    3) 나이대 정책 단일화:
       ```python
       AGE_BANDS: tuple[int, ...] = tuple(range(AGE_BAND_START, AGE_BAND_END, AGE_BAND_STEP))

       def in_age_band(age: int, band_start: int) -> bool:
           return band_start <= age < band_start + AGE_BAND_STEP
       ```
       - `_impute_weights`, `_aggregate_ratios`에서 `for a in AGE_BANDS` + `in_age_band`

    4) (선택) 캡슐화:
       - `ages`/`weights` 등 setter 차단 또는 `tuple` 복사본 반환

[F]
### 체크리스트
- [ ] 나이대 규칙 `a <= age < a+10`이 **한 함수**에만 정의
- [ ] impute·aggregate가 동일 `AGE_BANDS` 사용
- [ ] `shealth_bmi.py`의 `range(20, 80, 10)` → `AGE_BANDS` import (가능하면 본 Phase에서)

### 검증
```bash
python -m pytest src/test/python -v
```

### README To-Do 갱신
- [x] `@dataclass HealthRecord`
- [x] `AGE_BANDS` + `in_band`
- [x] mutable 리스트 캡슐화 (또는 부분 완료 명시)

### 커밋 메시지 초안
`refactor(shealth): introduce HealthRecord and centralize age band policy`
```

---

## 완료 기준

- [ ] Duplicated Code (나이대 루프 3중) 해소
- [ ] pytest Green
- [ ] Phase 06 진행 승인

---

*마지막 업데이트: 2026-05-20*
