# Phase 08 — 기타: 타입 힌트·PEP 8·dead code

> **README:** §2 기타 · §6 품질 게이트 (PEP 8)  
> **선행:** Phase 07 Green · **다음:** Activities 3 (pytest 확장) / Activities 4 (기능 개선)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. **동작 변경 없이** 가독성·정적 분석 품질만 높입니다.

[C] 제약
    - 공개 API·알고리즘 불변
    - 테스트 Green
    - Python 3.8: `list[int]`는 `from __future__ import annotations` 또는 `List[int]`

[T]
    1) 네이밍 (README §2 기타):
       - `a` → `age_band_start`
       - `total` (집계) → `band_size` / `valid_weight_count` 등 의미 구분
    2) `SHealth` 필드 type hint:
       ```python
       count: int
       _records: list[HealthRecord]
       _bmi_ratios: dict[tuple[int, int], float]
       ```
    3) 모든 public·private 메서드 인자/반환 hint
    4) unused import, dead code, 중복 상수 제거
    5) PEP 8: 줄 길이 88~99 이하, 4 spaces, import 순서 (stdlib → third → local)

[F]
### 체크리스트
- [ ] `shealth.py` / `shealth_bmi.py` public API 100% type hinted
- [ ] README To-Do §2 P1~P5·기타 미체크 항목 목록과 대조표 제출

### 검증
```bash
python -m pytest src/test/python -v
python -m pytest src/test/python --cov=src/main/python --cov-report=term-missing
```

### README To-Do 갱신
- [x] 네이밍 개선
- [x] dead code·불필요 import 제거

### 커밋 메시지 초안
`chore(shealth): complete type hints and PEP 8 cleanup`
```

---

## Phase 08 완료 후 권장 다음 작업

| Activities | README | task 폴더 (미작성) |
|------------|--------|-------------------|
| 3 | §3 pytest·커버리지 90% | `task_testing/` (별도) |
| 4 | §4 height 보정·전체 비율·정상 ID | `task_features/` (별도) |

---

## 완료 기준

- [ ] Activities 2 (1차 리팩토링) README §2 전체 [x] 가능
- [ ] pytest Green, 커버리지 베이스라인 기록

---

*마지막 업데이트: 2026-05-20*
