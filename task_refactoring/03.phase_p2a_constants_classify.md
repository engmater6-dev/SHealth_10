# Phase 03 — P2-A: 상수·`classify_bmi`·경계값 수정

> **README:** §2 P2 — `BmiThresholds`, `classify_bmi()`, `>= 25`  
> **선행:** Phase 02 Green · **다음:** [`04.phase_p2b_parser_enum.md`](04.phase_p2b_parser_enum.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. 매직 넘버를 이름 붙이고 분류 로직을 단일화합니다.

[C] 제약
    - `get_bmi_ratio(age, 100|200|300|400)` 호출·키 `(age_class, int)` 유지
    - `SHealth.UNDERWEIGHT = 100` 등 **정수 값 불변**
    - CSV 파서(`row[1..3]`)·빈 행 `break` — **이 Phase에서 변경 금지** (Phase 04)
    - Phase 01 경계 TC Green 필수

[T]
    1) 상수 모듈 또는 `shealth.py` 상단에 도입 (Python 3.8):
       ```python
       @dataclass(frozen=True)
       class BmiThresholds:
           underweight_max: float = 18.5
           normal_max: float = 23.0
           overweight_max: float = 25.0

       AGE_BAND_START, AGE_BAND_END, AGE_BAND_STEP = 20, 80, 10
       def age_bands() -> range: ...
       ```
       (`AgeBandPolicy`로 나이대 규칙 문서화 가능)

    2) **모듈 수준** 함수 추출:
       ```python
       def classify_bmi(bmi: float) -> int:
           """BMI → SHealth.UNDERWEIGHT|NORMALWEIGHT|OVERWEIGHT|OBESITY (100~400)."""
       ```
       - 저체중: `bmi <= 18.5`
       - 정상: `18.5 < bmi < 23`
       - 과체중: `23 <= bmi < 25`
       - 비만: `bmi >= 25`  ← **버그 수정: 기존 `> 25` 제거**

    3) `_aggregate_ratios` 내부 if/elif 4분기 → `classify_bmi(self.bmis[i])` + 카운터

    4) `range(20, 80, 10)` → `age_bands()` 또는 `AGE_BAND_*` 상수 사용 (집계·보정 루프)

[F]
### 체크리스트
- [ ] 임계값 18.5 / 23 / 25가 `BmiThresholds` 한 곳에만 존재
- [ ] BMI 25.0 → 비만(400) — TC #8, #21 Green
- [ ] `test_bmi_ratio_range` Green
- [ ] `shealth.dat` 스냅샷: 비만 비율만 변동 가능 — diff 문서화

### 검증
```bash
python -m pytest src/test/python -v
```

### README To-Do 갱신
- [x] `BmiThresholds` / `AgeBandPolicy`
- [x] `classify_bmi()` + `>= 25`

### 커밋 메시지 초안
`fix(shealth): extract classify_bmi and fix obesity boundary at 25.0`
```

---

## 완료 기준

- [ ] OCP 기반: 임계값 변경 시 한 모듈만 수정
- [ ] 경계 TC Green
- [ ] Phase 04 진행 승인

---

*마지막 업데이트: 2026-05-20*
