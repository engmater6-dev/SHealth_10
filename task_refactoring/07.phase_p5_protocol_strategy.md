# Phase 07 — P5: Protocol·전략 패턴 (확장성)

> **README:** §2 P5 — `Protocol`, `BmiClassifier`, (encoding은 Phase 04 완료 가정)  
> **선행:** Phase 06 Green · **다음:** [`08.phase_misc_pep8_types.md`](08.phase_misc_pep8_types.md)

---

## 프롬프트

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py

[P] Pythonic 리팩토링 코치입니다. OCP를 만족하는 **최소** 확장점을 추가합니다.

[C] 제약
    - 기본 동작 = 현재 WHO 아시아·태평양 임계값 (`BmiThresholds`와 동일)
    - `SHealth()` 무인자 생성 시 기존과 동일 결과
    - 과도한 DI·팩토리·추상 클래스 계층 **금지**
    - 테스트 Green

[T]
    1) `typing.Protocol` (`@runtime_checkable` 선택):
       ```python
       class SupportsBmiClassification(Protocol):
           def classify(self, bmi: float) -> BmiCategory: ...
       ```
       - (선택) `SupportsRecordReader`:
         ```python
         def read(self, path: Path) -> list[HealthRecord]: ...
         ```
       - `__subclasshook__` 필요 여부: duck typing만으로 충분하면 **미사용** (YAGNI)

    2) 전략 구현:
       ```python
       class WhoAsiaPacificClassifier:
           def __init__(self, thresholds: BmiThresholds | None = None): ...
           def classify(self, bmi: float) -> BmiCategory: ...
       ```
       - `classify_bmi()` 함수 본문을 이 클래스로 이전

    3) `SHealth.__init__(self, classifier: SupportsBmiClassification | None = None)`:
       - `self._classifier = classifier or WhoAsiaPacificClassifier()`
       - `_aggregate_ratios`에서 `self._classifier.classify(bmi)` 사용

    4) 모듈 수준 `classify_bmi(bmi)`는 thin wrapper로 유지 (하위 호환)

[F]
### 체크리스트
- [ ] 분류 정책 교체 시 `SHealth` 본문 수정 없이 Classifier만 교체 가능
- [ ] 단위 테스트에서 FakeClassifier 주입 예시 1건 (문서 또는 test)
- [ ] Protocol 문서 3줄: 왜 `isinstance` 대신 Protocol인지

### 검증
```bash
python -m pytest src/test/python -v
```

### README To-Do 갱신
- [x] `typing.Protocol`
- [x] 전략 패턴 `BmiClassifier.classify`

### 커밋 메시지 초안
`refactor(shealth): add BmiClassifier protocol and default WHO implementation`
```

---

## 완료 기준

- [ ] OCP: 분류 규칙 확장 시 기존 집계 코드 closed
- [ ] pytest Green
- [ ] Phase 08 진행 승인

---

*마지막 업데이트: 2026-05-20*
