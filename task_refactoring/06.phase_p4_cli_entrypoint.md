# Phase 06 — P4: CLI·진입점·로깅 (`shealth_bmi.py`)

> **README:** §2 P4 — 상수 사용, 포맷터, argparse, logging, type hints  
> **선행:** Phase 05 Green · **다음:** [`07.phase_p5_protocol_strategy.md`](07.phase_p5_protocol_strategy.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth_bmi.py @src/main/python/shealth.py

[P] Pythonic 리팩토링 코치입니다. 진입점과 도메인 계층의 중복·부수효과를 정리합니다.

[C] 제약
    - CLI **출력 라벨·소수 6자리** 형식 유지 (회귀 TC #44)
    - `calculate_bmi` 실패 시 `0` 반환 정책 유지
    - `get_bmi_ratio` API 불변
    - **권장:** 로깅 변경과 argparse 변경은 **커밋 2개**로 분리

[T] — **커밋 A: 출력·상수·포맷**
    1) `main`의 `100, 200, 300, 400` 제거:
       ```python
       from shealth import SHealth, BmiCategory  # 또는 SHealth.UNDERWEIGHT
       categories = (
           BmiCategory.UNDERWEIGHT,
           BmiCategory.NORMALWEIGHT,
           ...
       )
       ```
    2) `format_age_band_report(age: int, shealth: SHealth) -> str` 추출
    3) `for age in AGE_BANDS:` — `shealth.py`에서 import
    4) `def main() -> None:` + 모듈 docstring

[T] — **커밋 B: 경로·로깅**
    1) `argparse`:
       ```python
       parser.add_argument("data_file", nargs="?", default=Path("shealth.dat"), type=Path)
       ```
    2) `shealth.py` `FileNotFoundError`:
       - `print` 제거 → `logging.error("Failed to open file: %s", filename)`
       - `shealth_bmi.py`에서 `logging.basicConfig` 설정 (CLI만)
    3) (테스트) `caplog`로 ERROR 기록, print 없음 — TC #42

[F]
### 체크리스트
- [ ] `main`에 매직 넘버 100~400 없음
- [ ] `python shealth_bmi.py` / `python shealth_bmi.py custom.dat` 동작
- [ ] 도메인 모듈에 `print` 없음

### 검증
```bash
python -m pytest src/test/python -v
cd src/main/python && python shealth_bmi.py
```

### README To-Do 갱신
- [x] main 상수 사용
- [x] `format_age_band_report()`
- [x] argparse / Path
- [x] logging
- [x] `main() -> None`

### 커밋 메시지 초안
- A: `refactor(cli): use BmiCategory and extract age band report formatter`
- B: `refactor(cli): argparse data path and replace print with logging`
```

---

## 완료 기준

- [ ] SRP: CLI vs 도메인 분리
- [ ] pytest Green + 수동 CLI 확인
- [ ] Phase 07 진행 승인

---

*마지막 업데이트: 2026-05-20*
