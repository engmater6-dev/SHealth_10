# Phase 04 — P2-B: DictReader·빈 행·`BmiCategory` Enum

> **README:** §2 P2 — DictReader, `continue`, `BmiCategory`, encoding  
> **선행:** Phase 03 Green · **다음:** [`05.phase_p3_health_record.md`](05.phase_p3_health_record.md)

---

## 프롬프트

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. 파서 취약점과 매직 넘버(100~400)를 Enum으로 정리합니다.

[C] 제약
    - `BmiCategory.UNDERWEIGHT.value == 100` 등 **정수 값 동일**
    - `get_bmi_ratio(age, 100)` 기존 호출 코드 변경 없이 동작
    - `classify_bmi` 반환 타입은 `int` 또는 `BmiCategory`이되 `.value`로 dict 키 저장 시 호환
    - 테스트 Green

[T]
    1) 필드명 상수:
       ```python
       FIELD_ID, FIELD_AGE, FIELD_WEIGHT, FIELD_HEIGHT = "id", "age", "weight", "height"
       ```

    2) `_load_records`:
       - `csv.reader` + `row[1..3]` → `csv.DictReader`
       - `open(path, newline="", encoding="utf-8")`
       - 빈 행: `if not row or not any(cell.strip() for cell in row): continue`
       - **금지:** `if not row: break` (데이터 일부 누락 버그)

    3) `class BmiCategory(IntEnum):`
       ```python
       UNDERWEIGHT = 100
       NORMALWEIGHT = 200
       OVERWEIGHT = 300
       OBESITY = 400
       ```
       - `SHealth.UNDERWEIGHT = int(BmiCategory.UNDERWEIGHT)` 별칭 유지 또는 Enum 멤버 직접 참조

    4) `classify_bmi` → `BmiCategory` 반환으로 정리 (public API는 int 유지 가능)

[F]
### 체크리스트
- [ ] 헤더 `id,age,weight,height`와 DictReader 키 일치
- [ ] 중간 빈 행 CSV에서 `count`가 break 대비 증가하지 않거나, 증가 시 테스트·문서 반영
- [ ] `100~400` 리터럴이 집계·분류 코드에서 제거 (Enum만)

### 검증
```bash
python -m pytest src/test/python -v
```

### (권장) 추가 TC
- 빈 행 1줄 끼인 CSV → 이후 행 로드 (TC #37 확장)

### README To-Do 갱신
- [x] DictReader + 필드명 상수
- [x] 빈 행 `continue`
- [x] `BmiCategory` IntEnum
- [x] `encoding="utf-8"`

### 커밋 메시지 초안
`refactor(shealth): DictReader, skip blank rows, BmiCategory IntEnum`
```

---

## 완료 기준

- [ ] Fragile `row[i]` 제거
- [ ] pytest Green
- [ ] Phase 05 진행 승인

---

*마지막 업데이트: 2026-05-20*
