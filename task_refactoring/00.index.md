# task_refactoring — Phase별 리팩토링 프롬프트

> **기준:** [`README.md`](../README.md) To-Do §2 (Activities 2) · [`doc/code_quality_report.md`](../doc/code_quality_report.md) · [`doc/requirements_analysis.md`](../doc/requirements_analysis.md)  
> **대상 코드:** `src/main/python/shealth.py`, `src/main/python/shealth_bmi.py`  
> **작성일:** 2026-05-20

---

## 실행 순서

| 순번 | 파일 | README | 목표 | 커밋 단위 |
|------|------|--------|------|-----------|
| 0 | (본 문서) | §2 개요 | Phase 맵·공통 게이트 | — |
| 1 | [`01.phase_prerequisite_tests.md`](01.phase_prerequisite_tests.md) | §3 (선행) | 리팩토링 안전망 TC | 1 commit |
| 2 | [`02.phase_p1_calculate_bmi_split.md`](02.phase_p1_calculate_bmi_split.md) | P1 | `calculate_bmi` SRP 분해 | 1 commit |
| 3 | [`03.phase_p2a_constants_classify.md`](03.phase_p2a_constants_classify.md) | P2 (1/2) | 상수·`classify_bmi`·경계 `>=25` | 1 commit |
| 4 | [`04.phase_p2b_parser_enum.md`](04.phase_p2b_parser_enum.md) | P2 (2/2) | DictReader·빈 행·`BmiCategory` | 1 commit |
| 5 | [`05.phase_p3_health_record.md`](05.phase_p3_health_record.md) | P3 | dataclass·나이대 헬퍼 | 1 commit |
| 6 | [`06.phase_p4_cli_entrypoint.md`](06.phase_p4_cli_entrypoint.md) | P4 | CLI·로깅·argparse | 1~2 commit |
| 7 | [`07.phase_p5_protocol_strategy.md`](07.phase_p5_protocol_strategy.md) | P5 | Protocol·전략 패턴 | 1 commit |
| 8 | [`08.phase_misc_pep8_types.md`](08.phase_misc_pep8_types.md) | 기타 | 타입 힌트·PEP 8·dead code | 1 commit |

**원칙:** 각 Phase 완료 후 **반드시** `python -m pytest src/test/python -v` Green → 다음 Phase.

---

## README To-Do ↔ Phase 매핑

### P1 — `calculate_bmi` 분해

| To-Do | Phase |
|-------|-------|
| `load_records` | 02 |
| `impute_weights` | 02 (`_impute_heights`는 Activities 4 — 02에서 시그니처만 분리 가능) |
| `compute_bmis` | 02 |
| `aggregate_ratios` | 02 |

### P2 — 상수·파서·경계값

| To-Do | Phase |
|-------|-------|
| `BmiThresholds` / `AgeBandPolicy` | 03 |
| `classify_bmi()` + `>= 25` | 03 |
| `DictReader` + 필드명 상수 | 04 |
| 빈 행 `continue` | 04 |
| `BmiCategory` IntEnum | 04 |
| `encoding="utf-8"` | 04 또는 07 |

### P3 — 데이터 모델

| To-Do | Phase |
|-------|-------|
| `@dataclass HealthRecord` | 05 |
| `AGE_BANDS` + `in_band` | 05 |
| mutable 리스트 캡슐화 | 05 |

### P4 — 진입점

| To-Do | Phase |
|-------|-------|
| main 상수 사용 | 06 |
| `format_age_band_report()` | 06 |
| argparse / Path | 06 |
| `print` → `logging` | 06 (별 커밋 권장) |
| `main() -> None` | 06 |

### P5 — 확장성

| To-Do | Phase |
|-------|-------|
| `Protocol` | 07 |
| `BmiClassifier` 전략 | 07 |

### 기타

| To-Do | Phase |
|-------|-------|
| 네이밍·dead code | 08 |

---

## 공통 제약 (모든 Phase)

- **공개 API 하위 호환:** `calculate_bmi(filename: str) -> int`, `get_bmi_ratio(age_class: int, bmi_type: int) -> float`
- **BMI 타입 코드:** `100 / 200 / 300 / 400` 정수 값 유지 (`SHealth.UNDERWEIGHT` 등)
- **비율 범위:** 0.0 ~ 100.0 (`test_bmi_ratio_range` 유지)
- **테스트 Green에서만** 다음 Phase 진행
- **한 Phase = 한 PR/커밋** 권장 (범위 초과 시 Phase 내 하위 커밋만 허용)

---

## 공통 검증 명령

```bash
# 프로젝트 루트에서
python -m pytest src/test/python -v

# (선택) 커버리지 — Activities 3 이후
python -m pytest src/test/python --cov=src/main/python --cov-report=term-missing --cov-fail-under=90

# (선택) CLI 스모크 — P4 이후
cd src/main/python && python shealth_bmi.py
```

---

## 회귀 주의 (Golden)

| 변경 | 영향 |
|------|------|
| Phase 03: `>= 25` 비만 경계 | BMI=25.0 레코드 비율 변동 **의도적** — TC #8·#21·스냅샷 갱신 |
| Phase 04: 빈 행 `continue` | 중간 빈 행 이후 데이터 로드 건수 증가 가능 |
| Phase 04: DictReader | 컬럼명 불일치 시 파싱 실패 — 필드명 상수와 헤더 정합 확인 |

---

*마지막 업데이트: 2026-05-20*
