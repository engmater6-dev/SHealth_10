# Phase 01 — 리팩토링 선행: pytest 안전망

> **README:** §3 단위 테스트 (Activities 3) — **P1 직전** 권장  
> **선행 조건:** 없음 (현재 unittest 3건 Green)  
> **다음:** [`02.phase_p1_calculate_bmi_split.md`](02.phase_p1_calculate_bmi_split.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] pytest 기반 TDD 코치입니다. 리팩토링 전 회귀 방지용 최소 테스트를 추가합니다.

[C] 제약
    - 기존 `test_shealth_bmi.py` 3개 TC **Green 유지** (동작 변경 없음)
    - `@pytest.mark.skip` 사용 금지 (미구현 기능 제외)
    - Python 3.8 호환 (`from __future__ import annotations` 또는 typing)
    - `shealth.dat` 의존 TC는 유지하되, 신규 TC는 `tmp_path` fixture CSV 우선

[T] `doc/requirements_analysis.md` §4에서 **리팩토링 직후 깨지기 쉬운** TC만 우선 추가해줘.
    1) `test_classify_bmi_boundaries` (parametrize)
       - BMI 18.5 → 저체중(100), 18.500001 → 정상(200)
       - 23.0 → 과체중(300), 25.0 → 비만(400)  ← 회귀 핵심
    2) `test_impute_weight_by_age_band` — 20대 2건, w=0 → 동일 대 평균 (TC #10)
    3) (선택) `conftest.py` — `tmp_path`로 최소 CSV 헬퍼 fixture
    - `classify_bmi`가 아직 없으면 **테스트 먼저 작성 후 xfail 없이** Phase 03에서 구현 예정임을 주석으로 명시

[F]
### 체크리스트
- [ ] `python -m pytest src/test/python -v` 전체 Green
- [ ] 경계 TC #15~21 중 최소 4건 커버
- [ ] weight=0 보정 TC #10 커버

### 검증
```bash
python -m pytest src/test/python -v
```

### 산출물
- `src/test/python/conftest.py` (필요 시)
- `test_classify_bmi.py` 또는 `test_shealth_bmi.py` 확장
- README To-Do §3 해당 항목에 체크 가능한 범위 표시

### 커밋 메시지 초안
`test: add BMI boundary and weight imputation fixtures before refactor`
```

---

## 완료 기준

- [ ] 기존 3 TC + 신규 경계/보정 TC Green
- [ ] Phase 02 진행 승인

---

*마지막 업데이트: 2026-05-20*
