# Phase 02 — P1: `calculate_bmi` 분해 (SRP)

> **README:** §2 P1 — `load_records`, `impute_weights`, `compute_bmis`, `aggregate_ratios`  
> **선행:** Phase 01 권장 · **다음:** [`03.phase_p2a_constants_classify.md`](03.phase_p2a_constants_classify.md)

---

## 프롬프트

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. **동작 동일**한 구조 분해만 수행합니다.

[C] 제약
    - 공개 API `calculate_bmi`, `get_bmi_ratio` 시그니처·의미 불변
    - BMI 분류 경계(`> 25`), `row[1..3]`, `break` 빈 행 — **이 Phase에서 수정 금지**
    - `height=0` 보정 로직 **추가 금지** (Activities 4) — `_impute_heights`는 빈 stub 또는 미추출
    - 테스트 Green 후 완료

[T] `calculate_bmi`를 오케스트레이션만 남기고 private 메서드로 분해해줘.
    | 메서드 | 책임 | README To-Do |
    |--------|------|--------------|
    | `_reset_state()` | count·리스트·`_bmi_ratios` 초기화 | load 전처리 |
    | `_load_records(filename)` | CSV 읽기, 리스트 채우기, count 반환 | `load_records` |
    | `_impute_weights()` | weight=0 → 나이대 평균 (기존 알고리즘 그대로) | `impute_weights` |
    | `_compute_bmis()` | list comprehension BMI 산출 | `compute_bmis` |
    | `_aggregate_ratios()` | 나이대별 4범주 비율 dict | `aggregate_ratios` |

    `calculate_bmi` 본문 예시 흐름:
    ```python
    def calculate_bmi(self, filename: str) -> int:
        self._reset_state()
        if self._load_records(filename) == 0 and not self.ages:
            return 0  # FileNotFound 등 기존 반환 정책 유지
        self._impute_weights()
        self._compute_bmis()
        self._aggregate_ratios()
        return self.count
    ```
    - 각 메서드에 docstring 1줄 + type hint (인자/반환)
    - **로직 이동만** — 조건문·상수값 변경 없음

[F]
### 체크리스트
- [ ] `calculate_bmi` 본문이 5단계 이하로 읽히는가
- [ ] `shealth.dat` → `count > 0`
- [ ] `get_bmi_ratio(20, 100)` 등 기존 값과 **리팩토링 전 동일** (수동 또는 스냅샷)

### 검증
```bash
python -m pytest src/test/python -v
```

### README To-Do 갱신 (완료 시)
- [x] `load_records` / `impute_weights` / `compute_bmis` / `aggregate_ratios`

### 커밋 메시지 초안
`refactor(shealth): split calculate_bmi into load, impute, compute, aggregate`
```

---

## 완료 기준

- [ ] God Method 제거 (SRP 1차 달성)
- [ ] pytest Green
- [ ] Phase 03 진행 승인

---

*마지막 업데이트: 2026-05-20*
