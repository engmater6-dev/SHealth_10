# 02. 리팩토링 준비 — 프롬프트 등록

> `report/02.code_smell.md` 이후 단계 | README To-Do §2 기반 Phase별 리팩토링 프롬프트 설계  
> **실행용 상세본:** [`task_refactoring/`](../task_refactoring/) (00~08)  
> 작성일: 2026-05-20

---

## [02-01] Pythonic 리팩토링 계획 수립 (마스터)

```
@README.md @doc/code_quality_report.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다.

[C] 제약
    - 공개 API 하위 호환: `SHealth.calculate_bmi`, `SHealth.get_bmi_ratio` 유지
    - BMI 타입 코드 100/200/300/400 정수 값 유지
    - BMI 비율 0.0~100.0 범위 유지 (`test_bmi_ratio_range`)
    - 테스트 Green에서만 다음 단계 진행

[T] `SHealth` 클래스를 점진적으로 리팩토링하는 계획을 제안해줘.
    - 작은 단계 (커밋 단위)로 쪼개기
    - 전략 패턴: BMI 분류·나이대 정책을 교체 가능한 함수/클래스로 분리
    - dataclass 또는 Protocol 기반 타입 분리 가능성
    - 매직 넘버 상수화 (18.5, 23, 25, 20~70대, 100~400)
    - PEP 8 / 타입 힌트 보강
    - README To-Do P1→P2→P3→P4→P5 순서 준수

[F] 단계별 체크리스트 + 각 단계 검증 방법 (`python -m pytest src/test/python -v`)
    코드 변경·구현은 하지 말고 프롬프트·계획만 작성
```

---

## [02-02] task_refactoring 폴더 Phase별 등록

```
@README.md To Do list의 리팩토링을 참조하여 Phase별 프롬프트 검토하고
task_refactoring 폴더에 numbering해서 추가해줘

- README §2 P1~P5 · 기타 항목과 1:1 매핑
- Phase당 1커밋 원칙, pytest Green 게이트
- P2는 상수/분류(03)와 파서/Enum(04)로 분리
- Phase 01: P1 직전 pytest 안전망 (선행 권장)
```

---

## [02-03] 프롬프트·보고서 등록 (본 문서)

```
현재까지 프롬프트 내용만 prompt 폴더에 02.리팩토링 준비.md에 작성해주고,
report 폴더에 02.리팩토링 준비.md 파일에 보고서 작성해줘
```

---

## [02-04] Phase 01 — pytest 안전망 (선행)

> 상세: [`task_refactoring/01.phase_prerequisite_tests.md`](../task_refactoring/01.phase_prerequisite_tests.md)

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] pytest 기반 TDD 코치입니다. 리팩토링 전 회귀 방지용 최소 테스트를 추가합니다.

[C] 제약
    - 기존 `test_shealth_bmi.py` 3개 TC Green 유지
    - `@pytest.mark.skip` 사용 금지
    - Python 3.8 호환
    - 신규 TC는 `tmp_path` fixture CSV 우선

[T] `doc/requirements_analysis.md` §4에서 리팩토링 직후 깨지기 쉬운 TC만 우선 추가.
    1) `test_classify_bmi_boundaries` (parametrize) — 18.5, 23.0, 25.0 경계
    2) `test_impute_weight_by_age_band` — TC #10
    3) (선택) `conftest.py` — CSV 헬퍼 fixture

[F] 체크리스트 + `python -m pytest src/test/python -v` Green
    커밋: `test: add BMI boundary and weight imputation fixtures before refactor`
```

---

## [02-05] Phase 02 — P1 `calculate_bmi` 분해

> 상세: [`task_refactoring/02.phase_p1_calculate_bmi_split.md`](../task_refactoring/02.phase_p1_calculate_bmi_split.md)

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치 — 동작 동일한 구조 분해만.

[C] 공개 API 불변. `>25`, `row[1..3]`, `break` 빈 행 수정 금지. height 보정 추가 금지.

[T] private 메서드 분해:
    `_reset_state`, `_load_records`, `_impute_weights`, `_compute_bmis`, `_aggregate_ratios`
    `calculate_bmi`는 오케스트레이션만. 로직 이동만.

[F] pytest -v Green. 커밋: `refactor(shealth): split calculate_bmi into load, impute, compute, aggregate`
```

---

## [02-06] Phase 03 — P2-A 상수·`classify_bmi`·`>=25`

> 상세: [`task_refactoring/03.phase_p2a_constants_classify.md`](../task_refactoring/03.phase_p2a_constants_classify.md)

```
@README.md @doc/requirements_analysis.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치 — 매직 넘버 이름 붙이기·분류 단일화.

[C] `get_bmi_ratio(age, 100|200|300|400)` 유지. DictReader·빈 행은 Phase 04.

[T]
    1) `BmiThresholds` dataclass, `AGE_BAND_*` 상수
    2) `classify_bmi(bmi) -> int` — 비만 `bmi >= 25` 버그 수정
    3) `_aggregate_ratios`에서 `classify_bmi` 사용

[F] pytest -v Green. TC #8·#21. 커밋: `fix(shealth): extract classify_bmi and fix obesity boundary at 25.0`
```

---

## [02-07] Phase 04 — P2-B DictReader·Enum·빈 행

> 상세: [`task_refactoring/04.phase_p2b_parser_enum.md`](../task_refactoring/04.phase_p2b_parser_enum.md)

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치 — 파서·Enum 정리.

[C] `BmiCategory` 값 100/200/300/400 불변. 테스트 Green.

[T]
    1) 필드명 상수 + `csv.DictReader`
    2) 빈 행 `continue` (`break` 제거)
    3) `BmiCategory(IntEnum)`, `encoding="utf-8"`

[F] pytest -v Green. 커밋: `refactor(shealth): DictReader, skip blank rows, BmiCategory IntEnum`
```

---

## [02-08] Phase 05 — P3 `HealthRecord`·나이대 통합

> 상세: [`task_refactoring/05.phase_p3_health_record.md`](../task_refactoring/05.phase_p3_health_record.md)

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] Pythonic 리팩토링 코치 — Primitive Obsession·중복 루프 제거.

[C] 공개 API·`_bmi_ratios` 키 `(int,int)` 유지.

[T]
    1) `@dataclass HealthRecord`
    2) `AGE_BANDS` + `in_age_band(age, band_start)`
    3) impute·aggregate에서 단일 나이대 정책 사용
    4) (선택) mutable 리스트 캡슐화

[F] pytest -v Green. 커밋: `refactor(shealth): introduce HealthRecord and centralize age band policy`
```

---

## [02-09] Phase 06 — P4 CLI·로깅·argparse

> 상세: [`task_refactoring/06.phase_p4_cli_entrypoint.md`](../task_refactoring/06.phase_p4_cli_entrypoint.md)

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth_bmi.py @src/main/python/shealth.py

[P] Pythonic 리팩토링 코치 — 진입점·도메인 분리.

[C] CLI 출력 형식(6자리) 유지. `calculate_bmi` 실패 시 0 반환 유지.

[T] 커밋 A: `BmiCategory` 사용, `format_age_band_report`, `main() -> None`
    커밋 B: `argparse`+`Path`, `print`→`logging` (TC #42)

[F] pytest -v + `python shealth_bmi.py` 스모크
```

---

## [02-10] Phase 07 — P5 Protocol·전략 패턴

> 상세: [`task_refactoring/07.phase_p5_protocol_strategy.md`](../task_refactoring/07.phase_p5_protocol_strategy.md)

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py

[P] Pythonic 리팩토링 코치 — 최소 OCP 확장점.

[C] `SHealth()` 무인자 시 기존과 동일. 과도한 DI 금지.

[T]
    1) `SupportsBmiClassification` Protocol
    2) `WhoAsiaPacificClassifier`
    3) `SHealth.__init__(classifier=None)` 주입

[F] pytest -v Green. 커밋: `refactor(shealth): add BmiClassifier protocol and default WHO implementation`
```

---

## [02-11] Phase 08 — 타입 힌트·PEP 8·dead code

> 상세: [`task_refactoring/08.phase_misc_pep8_types.md`](../task_refactoring/08.phase_misc_pep8_types.md)

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] Pythonic 리팩토링 코치 — 동작 변경 없이 품질만.

[C] 공개 API·알고리즘 불변. Python 3.8 type hint.

[T] 네이밍 개선, 필드·메서드 hint 전면, unused import 제거, PEP 8

[F] pytest -v + `--cov=src/main/python`. 커밋: `chore(shealth): complete type hints and PEP 8 cleanup`
```

---

## Phase 실행 순서 (요약)

| ID | Phase | README | task_refactoring |
|----|-------|--------|------------------|
| 02-04 | 01 | §3 선행 | `01.phase_prerequisite_tests.md` |
| 02-05 | 02 | P1 | `02.phase_p1_calculate_bmi_split.md` |
| 02-06 | 03 | P2-A | `03.phase_p2a_constants_classify.md` |
| 02-07 | 04 | P2-B | `04.phase_p2b_parser_enum.md` |
| 02-08 | 05 | P3 | `05.phase_p3_health_record.md` |
| 02-09 | 06 | P4 | `06.phase_p4_cli_entrypoint.md` |
| 02-10 | 07 | P5 | `07.phase_p5_protocol_strategy.md` |
| 02-11 | 08 | 기타 | `08.phase_misc_pep8_types.md` |

**공통 검증:** `python -m pytest src/test/python -v`

---

*마지막 업데이트: 2026-05-20*
