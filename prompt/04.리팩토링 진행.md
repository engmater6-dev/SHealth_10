# 04. 리팩토링 진행 — 프롬프트 등록

> `report/03.리팩토링 준비.md` 이후 단계 | Activities 2 — Phase 02~08 실행  
> 작성일: 2026-05-20

---

## [04-01] Phase 02 — P1: `calculate_bmi` 분해 (SRP)

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. **동작 동일**한 구조 분해만 수행합니다.

[C] 제약
    - 공개 API `calculate_bmi`, `get_bmi_ratio` 시그니처·의미 불변
    - BMI 분류 경계(`> 25`), `row[1..3]`, `break` 빈 행 — **이 Phase에서 수정 금지**
    - `height=0` 보정 로직 **추가 금지** (Activities 4)
    - 테스트 Green 후 완료

[T] `calculate_bmi`를 오케스트레이션만 남기고 private 메서드로 분해:
    `_reset_state`, `_load_records`, `_impute_weights`, `_compute_bmis`, `_aggregate_ratios`
    - 각 메서드 docstring 1줄 + type hint
    - **로직 이동만** — 조건문·상수값 변경 없음

[F] pytest Green · `shealth.dat` count·비율 회귀 없음
커밋: `refactor(shealth): split calculate_bmi into load, impute, compute, aggregate`
```

---

## [04-02] Phase 03 — P2-A: 상수·`classify_bmi`·경계 `>= 25`

```
@README.md @doc/requirements_analysis.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. 매직 넘버를 이름 붙이고 분류 로직을 단일화합니다.

[C] 제약
    - `get_bmi_ratio(age, 100|200|300|400)` 키 유지
    - CSV 파서·빈 행 `break` — **이 Phase에서 변경 금지** (Phase 04)
    - Phase 01 경계 TC Green 필수

[T]
    1) `BmiThresholds` dataclass (18.5, 23, 25) + `AGE_BAND_*` / `age_bands()`
    2) `classify_bmi(bmi)` 모듈 함수 — 비만 `bmi >= 25` (**버그 수정**)
    3) `_aggregate_ratios` → `classify_bmi` 사용

[F] BMI 25.0 → 비만(400) Green
커밋: `fix(shealth): extract classify_bmi and fix obesity boundary at 25.0`
```

---

## [04-03] Phase 04 — P2-B: DictReader·빈 행·`BmiCategory`

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/test/python/test_shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. 파서 취약점과 매직 넘버(100~400)를 Enum으로 정리합니다.

[C] 제약
    - `BmiCategory` 값 100~400 불변 · `get_bmi_ratio` 호환
    - 테스트 Green

[T]
    1) `FIELD_ID`, `FIELD_AGE`, `FIELD_WEIGHT`, `FIELD_HEIGHT`
    2) `csv.DictReader` + `encoding="utf-8"` · 빈 행 `continue` (**break 금지**)
    3) `class BmiCategory(IntEnum)` · `classify_bmi` → `BmiCategory` 반환

[F] 빈 행 끼인 CSV TC · Fragile `row[i]` 제거
커밋: `refactor(shealth): DictReader, skip blank rows, BmiCategory IntEnum`
```

---

## [04-04] Phase 05 — P3: `HealthRecord`·나이대 통합

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] Pythonic 리팩토링 코치입니다. Primitive Obsession·중복 나이대 루프를 제거합니다.

[T]
    1) `models.py` — `@dataclass HealthRecord`
    2) `self._records` 주 저장소 · property 하위 호환 (`ages`, `weights` …)
    3) `AGE_BANDS` + `in_age_band()` · `_records_in_band()` (선택)

[F] 나이대 규칙 단일 함수 · pytest Green
커밋: `refactor(shealth): introduce HealthRecord and centralize age band policy`
```

---

## [04-05] Phase 06 — P4: CLI·로깅·argparse

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth_bmi.py @src/main/python/shealth.py

[P] Pythonic 리팩토링 코치입니다. 진입점과 도메인 계층의 중복·부수효과를 정리합니다.

[C] CLI 출력 라벨·소수 6자리 유지 · 도메인 `print` 금지

[T] — 커밋 A: `BmiCategory`, `format_age_band_report()`, `AGE_BANDS`, `main() -> None`
[T] — 커밋 B: `argparse` + `Path`, `logging.error` (FileNotFound), `caplog` TC #42

[F] pytest Green · CLI 스모크
```

---

## [04-06] Phase 07 — P5: Protocol·전략 패턴

```
@README.md @doc/code_quality_report.md
@src/main/python/shealth.py

[P] OCP를 만족하는 **최소** 확장점 추가. 과도한 DI 금지.

[T]
    1) `SupportsBmiClassification` Protocol
    2) `WhoAsiaPacificClassifier` — `classify_bmi` 본문 이전
    3) `SHealth(classifier=None)` 주입 · FakeClassifier 테스트 1건

[F] 무인자 `SHealth()` 동일 결과 · pytest Green
커밋: `refactor(shealth): add BmiClassifier protocol and default WHO implementation`
```

---

## [04-07] Phase 08 — 타입 힌트·PEP 8·dead code

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py

[P] **동작 변경 없이** 가독성·정적 분석 품질만 향상.

[T]
    1) 네이밍 (`age_band_start`, `band_size`, `valid_weight_count`)
    2) `SHealth` 필드·전 메서드 type hint
    3) unused import·dead code 제거 · PEP 8

[F] pytest Green · 커버리지 90% 베이스라인 · README §2 To-Do 대조
커밋: `chore(shealth): complete type hints and PEP 8 cleanup`
```

---

## [04-08] 프롬프트·보고서 등록 (본 문서)

```
현재까지 프롬프트 내용만 prompt 폴더에 04.리팩토링 진행.md에 작성해주고,
report 폴더에 04.리팩토링 진행.md 파일에 보고서 작성해줘
```

---

*마지막 업데이트: 2026-05-20*
