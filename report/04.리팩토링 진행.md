# 04. 리팩토링 진행 단계 — 작업 보고서

> **프로젝트:** SHealth BMI (Python)  
> **단계:** 04. 리팩토링 진행 (Activities 2 — Phase 02~08)  
> **작성일:** 2026-05-20  
> **선행 보고서:** [`report/03.리팩토링 준비.md`](03.리팩토링 준비.md)  
> **참조 프롬프트:** [`prompt/04.리팩토링 진행.md`](../prompt/04.리팩토링 진행.md)

---

## 1. 보고서 요약

Phase 01(준비) 이후 **Phase 02~08**을 순차 실행하여 README **Activities 2(1차 리팩토링)** 를 완료했습니다.  
공개 API는 유지했고, pytest **26건 Green**, 라인 커버리지 **92%**를 달성했습니다.

| 구분 | 상태 |
|------|------|
| P1 `calculate_bmi` 분해 | 완료 |
| P2 상수·파서·경계값 | 완료 |
| P3 `HealthRecord` | 완료 |
| P4 CLI·logging | 완료 |
| P5 Protocol·전략 | 완료 |
| 기타 type hint·PEP 8 | 완료 |
| pytest | **26 passed** |
| 커버리지 | **92%** (`--cov-fail-under=90` 충족) |

---

## 2. Phase별 수행 내역

### 2.1 Phase 02 — P1: `calculate_bmi` 분해

| 항목 | 내용 |
|------|------|
| 변경 | God Method → 5단계 오케스트레이션 |
| private 메서드 | `_reset_state`, `_load_records`, `_impute_weights`, `_compute_bmis`, `_aggregate_ratios` |
| 제약 준수 | `> 25`, `row[i]`, `break` **미변경** |
| 회귀 | `shealth.dat` count 4822, 나이대별 비율 동일 |

### 2.2 Phase 03 — P2-A: 상수·분류·경계 수정

| 항목 | 내용 |
|------|------|
| 추가 | `BmiThresholds`, `classify_bmi()`, `WhoAsiaPacificClassifier` 전신 |
| 버그 수정 | BMI **25.0** 비만 누락 (`> 25` → `>= 25`) |
| 테스트 | `bmi_25.0_obesity` **Green** |
| 실데이터 | `shealth.dat` 비율 변동 없음 (BMI 정확히 25.0 레코드 0건) |

### 2.3 Phase 04 — P2-B: DictReader·Enum

| 항목 | 내용 |
|------|------|
| 파서 | `csv.DictReader` + `FIELD_*` 상수 + `utf-8` |
| 빈 행 | `break` → `continue` (`_is_blank_row`) |
| Enum | `BmiCategory` IntEnum (100~400) |
| TC | `test_load_records_skips_blank_row_in_middle` |

### 2.4 Phase 05 — P3: `HealthRecord`

| 항목 | 내용 |
|------|------|
| 모델 | `models.py` — `HealthRecord` |
| 저장소 | `self._records` (평행 리스트 제거) |
| 나이대 | `AGE_BANDS`, `in_age_band()`, `_records_in_band()` |
| 호환 | `ages`/`weights`/`heights`/`bmis` property |
| 이슈 | `id` 과학적 표기법(`1e+05`) → `int(float(...))` 처리 |

### 2.5 Phase 06 — P4: CLI

| 항목 | 내용 |
|------|------|
| CLI | `format_age_band_report()`, `BmiCategory`, `AGE_BANDS` |
| 인자 | `argparse` + `Path` 기본값 `shealth.dat` |
| 로깅 | 도메인 `print` → `logger.error` · CLI `basicConfig` |
| TC | `test_cli_logging.py` (TC #42, #44) |

### 2.6 Phase 07 — P5: Protocol

| 항목 | 내용 |
|------|------|
| Protocol | `SupportsBmiClassification` |
| 전략 | `WhoAsiaPacificClassifier`, `SHealth(classifier=...)` |
| TC | `AllObesityClassifier` Fake 주입 |

### 2.7 Phase 08 — 기타

| 항목 | 내용 |
|------|------|
| 네이밍 | `age_band_start`, `band_size`, `valid_weight_count` |
| 타입 | `SHealth` 필드·전 메서드 hint |
| 정리 | `List`/`Dict` → builtin, import 순서 |
| README | §2 Activities 2 To-Do `[x]` 갱신 |

---

## 3. 최종 코드 구조

```
src/main/python/
  models.py          HealthRecord
  shealth.py         SHealth, BmiCategory, BmiThresholds, Classifier, 정책
  shealth_bmi.py     CLI (argparse, format_age_band_report)
src/test/python/     26 tests (unittest 3 + pytest 23)
```

---

## 4. 테스트·품질 지표

| 지표 | 결과 |
|------|------|
| pytest | 26 passed |
| 커버리지 (전체) | 92% |
| `shealth.py` | 98% |
| `models.py` | 100% |
| `shealth_bmi.py` | 63% (`main` 미실행) |

**주요 테스트 파일**

| 파일 | 역할 |
|------|------|
| `test_classify_bmi_boundaries.py` | 경계 통합 |
| `test_classify_bmi_unit.py` | `classify_bmi` 단위 |
| `test_impute_weight.py` | weight 보정 |
| `test_load_records.py` | 빈 행 continue |
| `test_bmi_classifier.py` | Protocol·Fake |
| `test_cli_logging.py` | logging·포맷 |
| `test_age_band_policy.py` | `in_age_band` |

---

## 5. 확인된 이슈·잔여 과제

| 우선순위 | 이슈 | 비고 |
|----------|------|------|
| 중간 | `_impute_heights` 미구현 | Activities 4 |
| 중간 | CSV 잘못된 행 방어·WARNING | Activities 4 |
| 낮음 | CLI 기본 경로 CWD 의존 | 루트 vs `src/main/python` |
| 낮음 | `SupportsRecordReader` 미구현 | P5 선택(YAGNI) |
| 낮음 | 부동소수점 BMI 25.0 (계산 경로) | 단위 vs weight/height 역산 |

---

## 6. README To-Do (Activities 2) 완료 현황

| 구간 | 완료 | 미완 (의도적) |
|------|------|----------------|
| P1 | 5/6 | `_impute_heights` → Activities 4 |
| P2 | 전체 | — |
| P3 | 전체 | — |
| P4 | 전체 | — |
| P5 | 2/3 | `SupportsRecordReader` 선택 |
| 기타 | 전체 | — |

---

## 7. 다음 단계

| Activities | 내용 |
|------------|------|
| 3 | pytest TC 확장·커버리지 90% 게이트 유지 |
| 4 | height 보정, 전체 비율, 정상 ID 목록 |
| 5 | 회고·Before/After |

---

*마지막 업데이트: 2026-05-20*
