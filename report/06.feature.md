# 06. Feature — 작업 보고서 (Activities 4 기능 개선)

> **프로젝트:** SHealth BMI (Python)  
> **단계:** 06. Feature / Activities 4 (2시간)  
> **작성일:** 2026-05-20  
> **참조:** [`prompt/06.feature.md`](../prompt/06.feature.md) · [`doc/feature_plan.md`](../doc/feature_plan.md) · [`doc/feature_policy.md`](../doc/feature_policy.md)

---

## 1. 보고서 요약

Activities 4 범위의 **height 보정**, **신규 통계 API 2종**, **CSV 방어 파싱**을 구현하고 pytest 17건을 추가했습니다.  
Golden Master(`shealth.dat`)는 height=0 **0건**으로 **변경 없이** Green입니다.

| 구분 | 상태 |
|------|------|
| 기능 정책 | `doc/feature_policy.md` |
| `shealth.py` 확장 | `_impute_heights`, 신규 API 2, `_parse_health_row` |
| pytest | **62건** unit (+2 golden) · 커버리지 **99%** |
| Golden TC #44 | **불변** (갱신 없음) |
| 모듈 분리 (Phase 08) | **YAGNI 보류** |

---

## 2. Before / After

| 항목 | Before | After |
|------|--------|-------|
| height=0 보정 | ✗ | `_impute_heights()` (weight 대칭) |
| 전체 BMI 비율 | ✗ | `get_overall_bmi_distribution()` |
| 정상 ID 목록 | ✗ | `get_normal_weight_user_ids()` |
| 잘못된 CSV 행 | 예외 전파 | skip + `WARNING` |
| pytest (unit) | 45건 | **62건** |
| `shealth.py` 커버리지 | 100% | **100%** |

---

## 3. Phase별 수행 결과

| Phase | 결과 | 산출물 |
|-------|------|--------|
| 01 | ✅ | `doc/feature_policy.md` |
| 02 | ✅ | `_impute_heights`, `calculate_bmi` 연결 |
| 03 | ✅ | `_parse_health_row`, 행 단위 warning |
| 04 | ✅ | `get_overall_bmi_distribution()` |
| 05 | ✅ | `get_normal_weight_user_ids()` |
| 06 | ✅ | 테스트 5파일 · 17 TC |
| 07 | ✅ | golden 2건 Green, cov 99% |
| 08 | ✅ B | 모듈 분리 **미실시** (YAGNI) |
| 09 | ✅ | 본 보고서, `prompt/06.feature.md`, README |

---

## 4. 신규 API

| 메서드 | 설명 |
|--------|------|
| `get_overall_bmi_distribution()` | `height>0` 레코드 기준 4범주 비율(%) |
| `get_normal_weight_user_ids()` | `18.5 < BMI < 23` 인 `id` 목록 (입력 순서) |

파이프라인: `load → impute weight → impute height → compute BMI → aggregate`

---

## 5. 신규 테스트 파일

| 파일 | TC |
|------|-----|
| `test_impute_height.py` | #23~26 |
| `test_overall_distribution.py` | #29~30 |
| `test_normal_weight_ids.py` | #32~35 |
| `test_load_records_invalid.py` | #38~39 |
| `test_age_out_of_band.py` | #40 |

---

## 6. Golden Master

- `shealth.dat`: 4822행, `height=0` **0건**
- `pytest -m golden_master`: **2 passed**, stdout golden **미갱신**

---

## 7. Phase 08 (YAGNI)

`Reader` / `Imputer` / `Statistics` 별도 모듈 분리는 **보류**했습니다.  
`SHealth`의 `_load_records`, `_impute_*`, `_aggregate_ratios`가 이미 SRP 1차를 만족하며, 실습 시간 대비 이득이 작다고 판단했습니다. `SupportsRecordReader`도 동일하게 미구현 유지.

---

## 8. 검증 명령

```bash
py -3 -m pytest -v
py -3 -m pytest -m "not golden_master" --cov=src/main/python --cov-fail-under=90
py -3 -m pytest -m golden_master -v
```

---

*마지막 업데이트: 2026-05-20*
