# Phase 05 — CSV 로드·예외·로깅

> **README:** §3 예외 TC · TC #36~37, #42 · (#38~39는 구현 후)  
> **선행:** Phase 04 Green  
> **다음:** [`06.phase_integration_ratio_smoke.md`](06.phase_integration_ratio_smoke.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/test_plan.md
@src/main/python/shealth.py @src/main/python/shealth_bmi.py
@src/test/python/conftest.py

[P] 시니어 QA 리드입니다. 방어적 I/O·로깅 검증에 집중합니다.

[C] `pytest` `caplog` · `logging` 레벨 · 도메인 `print` 금지

[T]
    1) `test_load_records.py`
       - TC #37: 헤더만 CSV → `calculate_bmi` return 0, 비율 조회 0.0
       - TC #36: 존재하지 않는 파일 → return 0, `caplog`에 ERROR, **print 없음**
       - 빈 행 중간 삽입 → 이후 행도 로드 (`continue`, not `break`) — TC #42 연계
    2) `test_cli_logging.py` (또는 통합)
       - `shealth_bmi.main` / `calculate_bmi` 경로에서 FileNotFound ERROR
    3) **예외·특이 표 (계획서 갱신용)** — 아래 케이스를 테스트 또는 백로그로 분류
       | 케이스 | TC # | 현재 | 조치 |
       |--------|------|------|------|
       | 잘못된 행 `abc,x,y` | 38 | 미구현 | Activities 4 또는 `@pytest.mark.xfail` **금지** — skip 없이 백로그 |
       | 컬럼 부족 3필드 | 39 | 미구현 | 동일 |
       | age=19 | 40 | 정책 확인 | assert 통계 제외 |
       | 음수 weight | 41 | 정책 확인 | |
    4) 구현되어 있으면 WARNING 스킵 로그 assert; 없으면 `doc/test_plan.md` P3 백로그만

[F]
### 체크리스트
- [ ] TC #36, #37, #42 Green
- [ ] caplog로 ERROR 검증
- [ ] #38~39는 테스트 파일에 **주석 TODO + 백로그** (skip 없음)

### 검증
```bash
python -m pytest src/test/python/test_load_records.py src/test/python/test_cli_logging.py -v
```
```

---

## 완료 기준

- [ ] 파일 없음·헤더만·빈 행 Green
- [ ] 예외 표가 계획서와 동기화

---

*마지막 업데이트: 2026-05-20*
