# Phase 07 — Protocol·FakeClassifier (OCP)

> **README:** §3 · P5 Protocol · TC 분류 전략 교체  
> **선행:** Phase 06 Green  
> **다음:** [`08.phase_coverage_gate.md`](08.phase_coverage_gate.md)

---

## 프롬프트

```
@README.md @src/main/python/shealth.py @src/test/python/conftest.py

[P] pytest + 타입(Protocol) 테스트에 익숙한 Python 아키텍트입니다.

[C] `SupportsBmiClassification` 주입 · 기존 WHO 분류 TC Green 유지

[T]
    1) `test_bmi_classifier.py`
       - `FakeClassifier`: 모든 BMI → `BmiCategory.OBESITY` (또는 고정 매핑)
       - `SHealth(classifier=FakeClassifier())` + 소형 CSV → 집계 비율이 **가짜 분류**를 따름
    2) 기본 `SHealth()` → `WhoAsiaPacificClassifier` 동작 회귀 1건
    3) `classify_bmi()` thin wrapper가 Classifier와 동일한지 1건

[F]
### 체크리스트
- [ ] 주입 시 집계 로직 변경 없이 분류만 교체됨을 증명
- [ ] Phase 03 경계 TC는 **기본 Classifier**로仍 Green

### 검증
```bash
python -m pytest src/test/python/test_bmi_classifier.py -v
python -m pytest src/test/python -v
```
```

---

## 완료 기준

- [ ] FakeClassifier 주입 TC Green
- [ ] 전체 suite Green

---

*마지막 업데이트: 2026-05-20*
