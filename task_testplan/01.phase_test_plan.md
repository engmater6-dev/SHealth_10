# Phase 01 — Activities 3 테스트 계획서 작성

> **README:** §3 단위 테스트 · Activities 3 (계획 선행)  
> **선행 조건:** [`doc/requirements_analysis.md`](../doc/requirements_analysis.md) 존재  
> **다음:** [`02.phase_pytest_bootstrap.md`](02.phase_pytest_bootstrap.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md
@src/main/python/shealth.py @src/main/python/models.py @src/main/python/shealth_bmi.py
@src/test/python/ @.cursorrules

[P] 시니어 QA 리드입니다.

[C] Python 3.8+, pytest, pytest-cov, (가능하면 coverage.py HTML 리포트)
    - 기존 unittest 3건 + pytest 23건·커버리지 92% 현황을 반영해 **갭·우선순위**를 명시
    - Activities 4 전용 TC(#23~35 일부)는 백로그로 분리

[T] Activities 3 테스트 계획서를 작성해줘.
    1) **범위·우선순위**
       - P0: 리팩토링 회귀 방지 (BMI 경계 18.5/23/25, weight=0, 빈 행 continue, ERROR 로깅)
       - P1: BMI 공식·cm→m (#1~2), `get_bmi_ratio` 0~100%, `shealth.dat` 스모크
       - P2: Protocol·FakeClassifier, 리포트 포맷
       - P3(백로그): height=0, 전체 비율, 정상 ID, 잘못된 행 스킵 (#38~39) — 구현 Phase와 매핑
    2) **@pytest.mark.parametrize 경계값 케이스 목록**
       - 표 형식: `bmi_input | expected BmiCategory | TC # | 비고`
       - 반드시 포함: 18.5, 18.500001, 22.999…, 23.0, 24.999…, 25.0, 극단값(9번)
       - weight/height 보정용 parametrize 후보 (나이대·0건·혼합 나이대)
    3) **예외·특이 케이스 목록**
       - 파일 없음, 헤더만, 빈 행 중간 삽입, 나이 19세/80세+, 보정 불가, caplog(ERROR/WARNING)
    4) **커버리지 목표·측정·개선 전략**
       - 목표: 라인 **≥ 90%** (`--cov-fail-under=90`)
       - 모듈별 목표: `shealth.py` ≥ 95%, `models.py` 100%, `shealth_bmi.py` CLI 분기
       - 미커버 라인 유형별 대응 (dead branch, main guard, FileNotFound 분기)
       - HTML 리포트 생성·리뷰 절차
    5) **테스트 파일 매핑** — `test_*.py` ↔ 담당 TC # ↔ Phase (task_testplan 02~08)

[F] Markdown 문서.
    - 저장 경로: `doc/test_plan.md`
    - 섹션: 개요 / 우선순위 / parametrize 표 / 예외 표 / 커버리지 / 실행 명령 / Phase 체크리스트
    - `doc/requirements_analysis.md` §4 TC 번호와 **1:1 추적** 표 포함
```

---

## 완료 기준

- [x] `doc/test_plan.md` 생성
- [ ] P0/P1 TC가 Phase 02~08 파일명과 연결됨
- [ ] parametrize·예외·커버리지 4개 블록이 모두 채워짐

---

## 검증

```bash
# 계획서만 작성하는 Phase — pytest Green은 Phase 02부터
dir doc\test_plan.md
```

---

*마지막 업데이트: 2026-05-20*
