# Phase 06 — 집계·비율·스모크 통합

> **README:** §3 분류·통합 TC · TC #27~28, #31, #43~44  
> **선행:** Phase 05 Green  
> **다음:** [`07.phase_classifier_protocol.md`](07.phase_classifier_protocol.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/test_plan.md
@src/main/python/shealth.py @shealth.dat @src/test/python/test_shealth_bmi.py

[P] 시니어 QA + 통합 테스트 설계자입니다.

[C] `shealth.dat`는 스모크·회귀에만 · 인메모리 CSV로 비율 합 검증

[T]
    1) `test_age_band_policy.py` — `in_age_band`, `AGE_BANDS` (TC 구간 정책)
    2) `get_bmi_ratio` 통합
       - TC #28: 빈 20대 → 0.0
       - TC #27: 20대 4명 각 범주 1명 → 각 25%, 합≈100 (`pytest.approx` sum)
       - 비율 0~100: `test_bmi_ratio_range` 유지·보강
    3) TC #43: N행 CSV → `calculate_bmi` return N
    4) TC #31: `shealth.dat` 스모크 — count>0, 모든 `get_bmi_ratio` in [0,100]
    5) TC #44 (선택): Golden 스냅샷 — 나이대별 6자리 문자열 fixture 비교 또는 허용 오차 ε
    6) `format_age_band_report` 단위 — 소수 6자리 포맷 1건

[F]
### 체크리스트
- [ ] 나이대 4종 합 100% (허용 오차 명시, 예: ±0.01)
- [ ] `test_shealth_bmi.py` unittest 3건 Green 유지
- [ ] `shealth.dat` 스모크 Green

### 검증
```bash
python -m pytest src/test/python -v -k "ratio or smoke or age_band"
python -m pytest src/test/python/test_shealth_bmi.py -v
```
```

---

## 완료 기준

- [ ] 집계·비율·스모크 TC Green
- [ ] unittest 3건 회귀 없음

---

*마지막 업데이트: 2026-05-20*
