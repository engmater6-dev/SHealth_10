# Phase 02 — pytest 부트스트랩·fixture

> **README:** §3 — pytest 확장, `conftest.py`, `tmp_path`  
> **선행:** Phase 01 계획서(권장) · 기존 `test_shealth_bmi.py` 3건 Green  
> **다음:** [`03.phase_bmi_formula_classify.md`](03.phase_bmi_formula_classify.md)

---

## 프롬프트

```
@README.md @doc/test_plan.md
@src/test/python/test_shealth_bmi.py @requirements-dev.txt

[P] pytest·fixture 설계에 익숙한 Python TDD 코치입니다.

[C] 제약
    - 기존 unittest 3 TC **동작·이름 유지** (Green)
    - `@pytest.mark.skip` 금지
    - Python 3.8 호환
    - `shealth.dat` 의존 TC는 기존 파일에만 두고, 신규 TC는 fixture CSV

[T]
    1) `requirements-dev.txt`에 `pytest`, `pytest-cov` 버전 명시 확인·보완
    2) `src/test/python/conftest.py` 작성:
       - `write_csv(tmp_path, rows: list[dict]) -> Path` — 헤더 `id,age,weight,height`
       - `shealth_from_csv(tmp_path, rows)` → `SHealth` + `calculate_bmi` 호출 헬퍼 (선택)
    3) README §3 실행 명령이 루트에서 동작하는지 주석으로 conftest에 명시
    4) Phase 03~05에서 재사용할 **최소 샘플 CSV** 2종 문서화 (20대 2건, 빈 행 1줄)

[F]
### 체크리스트
- [ ] `python -m pytest src/test/python -v` Green (기존 3+α)
- [ ] `conftest.py`에 fixture docstring
- [ ] README To-Do «pytest 확장» 체크 가능 범위 표시

### 검증
```bash
python -m pytest src/test/python -v
pip install -r requirements-dev.txt
```

### 커밋 메시지 초안
`test: add conftest CSV fixtures for pytest expansion`
```

---

## 완료 기준

- [ ] `conftest.py` 존재·재사용 가능
- [ ] 전체 suite Green

---

*마지막 업데이트: 2026-05-20*
