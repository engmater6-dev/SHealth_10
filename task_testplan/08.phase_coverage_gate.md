# Phase 08 — pytest-cov 커버리지 게이트·HTML·개선

> **README:** §3 라인 커버리지 90%+ · TC #45  
> **선행:** Phase 07 Green  
> **다음:** [`09.phase_register_prompt_report.md`](09.phase_register_prompt_report.md)

---

## 프롬프트

```
@README.md @.cursorrules @doc/test_plan.md
@src/main/python/ @src/test/python/

[P] 시니어 QA 리드입니다. 커버리지 거버넌스·개선 전략을 문서화합니다.

[C] pytest-cov, `--cov-fail-under=90`, HTML 리포트 (`htmlcov/`)

[T]
    1) **측정**
       ```bash
       python -m pytest src/test/python --cov=src/main/python --cov-report=term-missing --cov-fail-under=90
       python -m pytest src/test/python --cov=src/main/python --cov-report=html
       ```
    2) **개선 전략** (미커버 라인 유형별)
       - `if __name__ == "__main__"` / CLI `main()` guard → `# pragma: no cover` vs 직접 호출 TC
       - `FileNotFoundError` 분기 → Phase 05 TC
       - `_is_blank_row` false branch → 빈 행 아닌 정상 행 TC
       - `Optional`/`Protocol` 런타임 분기 없음 — 불필요한 100% 집창 금지
    3) **목표 표** — 모듈별 line % / 목표 / 갭 / 추가 TC 제안
       | 모듈 | 목표 | 조치 |
       |------|------|------|
       | shealth.py | ≥95% | … |
       | models.py | 100% | … |
       | shealth_bmi.py | ≥85% | main/argparse 1~2 TC |
    4) CI·로컬 동일 명령을 `doc/test_plan.md` §5에 반영
    5) HTML 리포트 리뷰 체크리스트 (빨간 줄 → TC # 매핑)

[F]
### 산출물
- `task_testplan/artifacts/08_coverage_report.md` (요약: 총 %, 미커버 라인 목록, 다음 TC 3건)
- `--cov-fail-under=90` **통과** 스크린샷 또는 터미널 로그 발췌

### 체크리스트
- [ ] 전체 line coverage ≥ 90%
- [ ] HTML `htmlcov/index.html` 생성 가능
- [ ] TC #45 충족
```

---

## 완료 기준

- [ ] `--cov-fail-under=90` Green
- [ ] `08_coverage_report.md` 작성

---

*마지막 업데이트: 2026-05-20*
