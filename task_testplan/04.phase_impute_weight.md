# Phase 04 — `weight=0` 나이대 평균 보정

> **README:** §3 weight 보정 · TC #10~14  
> **선행:** Phase 03 Green  
> **다음:** [`05.phase_load_records_exceptions.md`](05.phase_load_records_exceptions.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/test_plan.md
@src/main/python/shealth.py @src/test/python/conftest.py

[P] 시니어 QA 리드입니다. 데이터 보정 로직 검증에 집중합니다.

[C] `tmp_path` CSV only · `@pytest.mark.parametrize` for age-band cases

[T]
    1) `test_impute_weight.py` — TC #10 단일 누락
       - 20대: (w=60,h=170), (w=0,h=175) → 두 번째 weight=60, BMI 유효
    2) parametrize TC #11~12
       | id | age | weight | height | 기대 |
       |----|-----|--------|--------|------|
       | 다건 0 | 30대 w=50,80,0,0 | 0→65 | |
       | 나이대 격리 | 20대 w=0 + 30대 w=90 | 20대만 20대 평균 | |
    3) TC #13 보정 불가 — 40대 전원 w=0
       - 현재 구현 정책(0 유지 vs 제외)을 **assert로 고정**하고 계획서에 기록
    4) TC #14 — 보정 후 `ZeroDivisionError` 없음, `shealth.weights` property로 검증

[F]
### parametrize 후보 (계획서에 복사 가능)
```python
@pytest.mark.parametrize(
    "rows,age_band,idx_zero,expected_weight",
    [
        # (rows as minimal tuples), 20, 1, 60.0
    ],
)
```

### 체크리스트
- [ ] TC #10~14 중 구현된 정책에 맞는 TC Green
- [ ] `in_age_band` / `_records_in_band`와 보정 대상 일치

### 검증
```bash
python -m pytest src/test/python/test_impute_weight.py -v
```
```

---

## 완료 기준

- [ ] weight=0 보정 최소 3 시나리오 Green
- [ ] 나이대 혼합 시 격리 검증

---

*마지막 업데이트: 2026-05-20*
