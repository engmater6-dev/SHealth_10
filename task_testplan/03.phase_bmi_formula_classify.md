# Phase 03 — BMI 공식·경계 분류 (`parametrize`)

> **README:** §3 BMI 경계값 · TC #1~2, #15~22  
> **선행:** Phase 02 Green  
> **다음:** [`04.phase_impute_weight.md`](04.phase_impute_weight.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/test_plan.md
@src/main/python/shealth.py @src/test/python/conftest.py

[P] 시니어 QA + Python TDD 코치입니다.

[C] pytest, `@pytest.mark.parametrize`, `pytest.approx`
    - `classify_bmi()` / `WhoAsiaPacificClassifier` 단일 분류 경로만 검증
    - `BmiCategory` IntEnum 값: 100/200/300/400

[T]
    1) **BMI 공식 TC (#1~2)** — `test_bmi_formula.py` (신규)
       - weight=70, height=175(cm) → BMI ≈ 22.857… (`70/(1.75**2)`)
       - height 180 → m 변환 1.8 반영 (직접 `_compute_bmis` 또는 공개 경로)
    2) **경계 분류 parametrize (#15~21, #22 일부)** — `test_classify_bmi_boundaries.py`
       ```python
       @pytest.mark.parametrize(
           "bmi,expected",
           [
               (17.0, BmiCategory.UNDERWEIGHT),
               (18.5, BmiCategory.UNDERWEIGHT),
               (18.500001, BmiCategory.NORMALWEIGHT),
               (21.0, BmiCategory.NORMALWEIGHT),
               (22.999, BmiCategory.NORMALWEIGHT),
               (23.0, BmiCategory.OVERWEIGHT),
               (24.999, BmiCategory.OVERWEIGHT),
               (25.0, BmiCategory.OBESITY),
               (30.0, BmiCategory.OBESITY),
           ],
       )
       ```
       - `classify_bmi(bmi)` 및 `WhoAsiaPacificClassifier().classify(bmi)` **동일 결과**
    3) **회귀 핵심:** BMI=25.0 → OBESITY(400) — README 버그(`>25`) 재발 방지
    4) (선택) 100건 랜덤 BMI → 정확히 1개 범주 (#22) — `hypothesis` 없이 고정 시드

[F]
### 체크리스트
- [ ] TC #1~2, #15~21 커버
- [ ] parametrize 표가 `doc/test_plan.md`와 일치
- [ ] `pytest -v -k classify` Green

### 검증
```bash
python -m pytest src/test/python/test_bmi_formula.py src/test/python/test_classify_bmi_boundaries.py -v
```
```

---

## 완료 기준

- [ ] 경계 9+ 케이스 parametrize Green
- [ ] BMI=25.0 비만 분류 고정

---

*마지막 업데이트: 2026-05-20*
