# Phase 07 — Golden Master·스모크·커버리지 회귀

> **README:** §6 품질 게이트 · TC #31, #44, #45  
> **선행:** Phase 06 Green  
> **다음:** [`08.phase_optional_class_split.md`](08.phase_optional_class_split.md)

---

## 프롬프트

```
@README.md @task_golden_master/00.index.md @doc/golden_master_plan.md
@src/test/python/test_golden_master.py @src/test/golden/shealth_dat_stdout.txt
@shealth.dat @src/main/python/shealth_bmi.py

[P] 회귀·CI 담당 QA입니다. **의도적 통계 변경**만 golden을 갱신합니다.

[C] Golden approve: `UPDATE_GOLDEN=1` + 커밋 ([`task_golden_master/03.phase_golden_test.md`](../task_golden_master/03.phase_golden_test.md))
    - height=0이 `shealth.dat`에 **없으면** CLI 6줄 golden **불변**이 정상
    - height 보정이 실데이터 비율을 바꾸면 golden **재생성 + PR 설명** 필수

[T] Activities 4 마무리 회귀를 수행·문서화해줘.
    1) `shealth.dat`에서 height=0 건수 조사 (스크립트 또는 grep)
    2) Golden 비교
       ```bash
       py -3 -m pytest -m golden_master -v
       ```
       - 실패 시: diff 원인이 height 보정인지 다른 변경인지 분리
       - 필요 시만: `set UPDATE_GOLDEN=1` 후 재실행·`shealth_dat_stdout.txt` 커밋
    3) 통합 스모크 — TC #31
       - `calculate_bmi("shealth.dat")` > 0
       - `get_bmi_ratio(20~70, 100~400)` ∈ [0, 100]
       - (신규) `get_overall_bmi_distribution()` 합 ≈ 100%
    4) CI 워크플로 — unit+cov / golden-master job Green 확인
    5) `doc/defect_list.md` — Activities 4로 해소된 REQ 항목 1~2줄 (선택)

[F]
### 체크리스트
- [ ] `pytest -m "not golden_master"` + cov 90%+ Green
- [ ] `pytest -m golden_master` Green (또는 golden 미변경 근거 기록)
- [ ] README §4.3 나이대 4종 합 검증 `[x]` (이미 `test_bmi_ratio_sum`)

### 검증
```bash
py -3 -m pytest -v
py -3 -m pytest -m golden_master -v
py -3 -m pytest -m "not golden_master" --cov=src/main/python --cov-fail-under=90
```
```

---

## 완료 기준

- [ ] 전체 suite·CI 게이트 Green
- [ ] Golden 변경 시 재생성 절차·이유가 report/06에 1문단 이상 기록 (Phase 09)

---

*프롬프트만 등록 — 구현 미진행*
