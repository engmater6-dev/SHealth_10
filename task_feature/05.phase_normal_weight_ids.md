# Phase 05 — 정상 범위 사용자 ID 목록 API

> **README:** §4.3 `get_normal_weight_user_ids` · TC #32~35  
> **선행:** Phase 04 (동일 `record.bmi` 전제)  
> **다음:** [`06.phase_feature_tests.md`](06.phase_feature_tests.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/feature_policy.md
@src/main/python/shealth.py @src/main/python/models.py

[P] 도메인 API·경계값 QA에 집중하는 Python 개발자입니다.

[C] 제약
    - 정상 범위: **18.5 < BMI < 23** (README·TC #33 — 18.5·23.0 **포함 안 함**)
    - `WhoAsiaPacificClassifier` / `classify_bmi`와 **동일 임계값** (`BmiThresholds`)
    - 반환 `list[int]` — `HealthRecord.id` (int), **파일 입력 순서** 유지 (TC #35)
    - `calculate_bmi` 미호출 시 빈 리스트 또는 정책에 따른 동작 — docstring 명시

[T] `SHealth.get_normal_weight_user_ids()`를 구현해줘.
    1) `self._records` 순회, `record.bmi`가 정상 구간이면 `record.id` append
    2) 경계 케이스 (TC #33)
       - BMI=18.5 → 제외 (저체중 상한 포함 분류와 구분)
       - BMI=23.0 → 제외 (과체중 하한)
    3) 전원 비만/저체중 → `[]` (TC #34)
    4) 복수 정상 ID — CSV 등장 순서와 동일

[F]
### API 스케치
```python
def get_normal_weight_user_ids(self) -> list[int]:
    """BMI가 정상 범위(18.5 초과 23 미만)인 사용자 id를 입력 순서로 반환한다."""
    ...
```

### 체크리스트
- [ ] TC #32: id=1 BMI17 제외, id=2 BMI22 포함 → `[2]`
- [ ] 분류기 주입(`FakeClassifier`) 테스트는 Phase 06에서 선택

### 검증
```bash
py -3 -m pytest src/test/python -v
```

### 커밋 메시지 초안
`feat(shealth): add get_normal_weight_user_ids API`
```

---

## 완료 기준

- [ ] 공개 메서드 + type hint + docstring
- [ ] 경계 18.5·23.0 제외 규칙이 코드·문서에 일치

---

*프롬프트만 등록 — 구현 미진행*
