# Phase 04 — 전체 사용자 BMI 범주 비율 API

> **README:** §4.3 `get_overall_bmi_distribution` · TC #29~30  
> **선행:** Phase 02 BMI 유효 레코드 확정  
> **다음:** [`05.phase_normal_weight_ids.md`](05.phase_normal_weight_ids.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/feature_policy.md
@src/main/python/shealth.py
@src/test/python/test_bmi_ratio_sum.py

[P] 통계·집계 API 설계에 익숙한 Python 개발자입니다.

[C] 제약
    - `calculate_bmi` 호출 **후**에만 의미 있는 결과 (내부 상태 `_records`, `record.bmi` 사용)
    - 반환: `dict[BmiCategory, float]` — 각 값 0.0~100.0, **4범주 합 ≈ 100%** (TC #29)
    - 분모 N: Phase 01 정책 준수 (예: BMI 계산된 레코드만 vs 전체 count)
    - 나이대 필터 **없음** — 전체 유효 집단 (REQ-07)
    - `get_bmi_ratio` 시그니처·의미 **불변**

[T] `SHealth.get_overall_bmi_distribution()`을 구현해줘.
    1) `_aggregate_ratios`와 동일 분류기(`self._classifier`) 사용
    2) 집계 로직
       - 각 `record`에 대해 `classify(record.bmi)` → 카운트
       - `ratio = count / N * 100` for each `BmiCategory`
    3) N=0 → 네 범주 모두 `0.0` (TC #30은 N=1 케이스)
    4) (선택) CLI 노출 없음 — API·테스트만 (README Activities 4 목록과 정합)

[F]
### API 스케치
```python
def get_overall_bmi_distribution(self) -> dict[BmiCategory, float]:
    """전체 유효 레코드 기준 BMI 범주별 비율(%)을 반환한다."""
    ...
```

### 체크리스트
- [ ] TC #29: 10명 (2,3,3,2) → 20,30,30,20
- [ ] TC #30: 1명 정상 → 정상 100%, 나머지 0

### 검증
```bash
py -3 -m pytest src/test/python -v
```

### 커밋 메시지 초안
`feat(shealth): add get_overall_bmi_distribution API`
```

---

## 완료 기준

- [ ] 공개 메서드 + type hint + docstring
- [ ] Phase 06에서 TC #29~30 Green

---

*프롬프트만 등록 — 구현 미진행*
