# Phase 08 — Reader / Imputer / Statistics 분리 (선택)

> **README:** §4.4 Reader·Imputer·Statistics 별도 클래스 · YAGNI  
> **선행:** Phase 07 Green  
> **다음:** [`09.phase_register_prompt_report.md`](09.phase_register_prompt_report.md)

---

## 프롬프트

```
@README.md @doc/code_quality_report.md @doc/requirements_analysis.md §5.1
@src/main/python/shealth.py @src/main/python/models.py
@task_refactoring/07.phase_p5_protocol_strategy.md

[P] 아키텍처 리팩토링 코치입니다. **YAGNI** — 필요 시에만 분리합니다.

[C] 제약
    - 공개 API `SHealth.calculate_bmi`, `get_bmi_ratio`, 신규 API **시그니처 불변**
    - `SupportsRecordReader` Protocol — README **선택·미구현** — 본 Phase에서도 강제하지 않음
    - 한 번에 전 모듈 분리보다 **최소 추출** (예: `imputer.py`만) 허용
    - 모든 변경 후 pytest + golden Green

[T] 아래 **A 또는 B** 중 팀·시간에 맞게 선택해 실행·기록해줘.

    **A) 최소 분리 (권장 상한)**
    - `imputer.py`: `impute_weights_by_band`, `impute_heights_by_band` (records in-place)
    - `statistics.py`: age-band ratio + overall distribution
    - `shealth.py`: 파사드 — 기존 private 메서드는 thin delegate

    **B) YAGNI 유지**
    - 클래스 분리 **하지 않음**
    - `report/06.feature.md`에 "Reader/Imputer 분리 보류 — SHealth private 메서드로 충분" 근거 3문장

[F]
### 체크리스트 (A 선택 시)
- [ ] 순환 import 없음
- [ ] `models.py`만 공유 dataclass
- [ ] 커버리지 90%+ 유지

### 검증
```bash
py -3 -m pytest src/test/python -v
py -3 -m pytest -m golden_master -v
```

### 커밋 메시지 초안 (A)
`refactor(shealth): extract imputer and statistics modules`
```

---

## 완료 기준

- [ ] A 또는 B가 문서·코드에 명시됨
- [ ] pytest Green

---

*프롬프트만 등록 — 구현 미진행*
