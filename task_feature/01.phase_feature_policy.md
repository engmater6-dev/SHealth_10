# Phase 01 — Activities 4 기능 정책·정책서 작성

> **README:** §4.1 보정 불가 · §4.3 20세 미만·80세 이상  
> **선행:** Activities 2~3 완료 · [`doc/requirements_analysis.md`](../doc/requirements_analysis.md)  
> **다음:** [`02.phase_impute_heights.md`](02.phase_impute_heights.md)

---

## 프롬프트

```
@README.md @doc/requirements_analysis.md @doc/test_plan.md
@src/main/python/shealth.py @src/main/python/models.py
@src/test/python/test_impute_weight.py

[P] 시니어 도메인 분석가·QA 리드입니다. **구현 코드 없이** 정책만 확정합니다.

[C] Python 3.8+ · 기존 weight=0 보정(`_impute_weights`)과 **대칭** 규칙 우선
    - Activities 3에서 이미 고정된 weight 보정 불가 정책과 **일관성** 유지
    - Golden Master(`shealth.dat`)에 height=0 실데이터 여부는 파일 검토 후 명시

[T] Activities 4 구현 전 **기능 정책서**를 작성해줘.
    1) **보정 순서·불가 시 정책**
       - load → weight impute → height impute → BMI → aggregate
       - 나이대 내 유효 샘플 0건: weight/height 각각 (0 유지 vs 레코드 제외 vs 전체 평균 fallback) — **택일 1안** + 근거
       - weight·height 동시 0 (TC #24): 순서·최종 BMI 처리
       - 보정 후 height=0 → BMI 계산 **금지** (ZeroDivision) — 스킵 vs WARNING
    2) **나이대·통계 대상**
       - 20≤age<80: 기존 `AGE_BANDS` / `in_age_band` 유지
       - age<20, age≥80: 통계·보정·전체 비율·정상 ID에 **포함 여부** (README 미명시 → 1안 고정)
    3) **신규 API 계약 (초안)**
       - `get_overall_bmi_distribution() -> dict[BmiCategory, float]` — 분모 N 정의(전체 로드 건수 vs BMI 유효 건수)
       - `get_normal_weight_user_ids() -> list[int]` — `18.5 < BMI < 23`, 입력 순서, 경계 18.5·23.0 **제외** (TC #33)
    4) **잘못된 CSV 행** — 스킵+WARNING vs 전체 실패 중 **1안** (TC #38~39)
    5) **Golden Master 영향 예측** — height=0이 `shealth.dat`에 없으면 golden **불변** 명시

[F] Markdown.
    - 저장: `doc/feature_policy.md` (신규) 또는 `doc/requirements_analysis.md` §1.2.3 보강
    - 섹션: 보정 / 나이대 / API / 예외 / Golden / Phase 02~07 체크리스트
    - 각 정책 항목에 대응 **TC #** 표기
```

---

## 완료 기준

- [ ] 보정 불가·나이 구간·API 분모 정책이 **문장 1안**으로 확정됨
- [ ] Phase 02~06 프롬프트가 참조할 **결정 표** 존재
- [ ] Golden 변경 필요 여부가 yes/no로 기록됨

---

## 검증

```bash
# 정책서만 — pytest 변경 없음
dir doc\feature_policy.md
```

---

*프롬프트만 등록 — 구현 미진행*
